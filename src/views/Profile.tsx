import React from 'react';
import { View, Text } from 'react-native';

const Profile = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Bem-vindo à tela de início!</Text>
            {/* Aqui você pode adicionar os componentes e estilo necessários conforme o modelo da imagem */}
        </View>
    );
};

export default Profile;